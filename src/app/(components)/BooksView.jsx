"use client";

import React, { useState } from "react";
import { useRouter } from "next/navigation";
import { Playfair_Display } from "next/font/google";

const BooksView = ({ data }) => {
	return (
		<div className="text-[var(--eggshell)]">
			<h3>books</h3>
		</div>
	);
};

export default BooksView;
